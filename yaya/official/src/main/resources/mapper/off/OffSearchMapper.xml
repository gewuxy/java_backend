<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.medcn.official.dao.OffSearchDao" >

  <select id="findTopHost" resultType="cn.medcn.official.model.OffSearch">
    SELECT COUNT(search)AS num,search FROM t_off_search WHERE search_type = #{searchType} GROUP BY search ORDER BY num DESC
  </select>
</mapper>